The docker file in this directory is ONLY for the scraper.


[POST PROCESSING]

HTML Generation,
OCR,
Getting Data


Happens in the postprocessing folder, which has its own folder and its own docker-compose file.
